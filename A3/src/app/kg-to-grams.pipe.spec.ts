import { KgToGramsPipe } from './kg-to-grams.pipe';

describe('KgToGramsPipe', () => {
  it('create an instance', () => {
    const pipe = new KgToGramsPipe();
    expect(pipe).toBeTruthy();
  });
});
